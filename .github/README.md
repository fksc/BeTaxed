# CI/CD — BeTaxed

Workflows will land in a later issue. Branch flow matches Talent Journey:

```
Feature/DEV-xxx → PR → dev → (staging) → Release PR → main → (production)
```

See `.KB/GIT_STRATEGY.md`.
